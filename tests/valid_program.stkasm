# FILE: math.stkasm
# DESC: A simple math library that provides a 'multiply' function.

# --- DATA SECTION ---
# This library has its own private data.
.data
    .static version_number 100 # Represents version 1.00

# --- TEXT (CODE) SECTION ---
.text
    .global multiply

multiply:
    imul
    ret